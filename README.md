本脚本基于 AutoHotkey v2 编写，集成了智能划词、多态悬浮窗、AI 快捷上传、多击触发以及系统级的窗口管理功能。脚本具备高度的移植性，能自动适配不同的显示器 DPI 缩放并智能寻找依赖程序。

## 📁 一、 运行环境与前置准备

1. **基础环境**：需安装 **AutoHotkey v2.0** 及以上版本。
2. **浏览器依赖**：默认优先调用 **Chrome**，若未安装则自动无缝降级使用系统自带的 **Microsoft Edge**。
3. **目录结构**：确保脚本同级目录下存在 `resources\` 文件夹，并包含以下关键内容：
* `photo\`：存放悬浮窗图标（`gemini_prompt.png`, `gemini.png`, `youglish_auto_load.png`, `copied.png`）。
* `programfiles\`：存放便携版软件（如 Everything, Telegram）。
* `.bat` 批处理文件：存放网络切换、关机、重启等脚本。
* `GeminiPromptEditor.exe` 与 `active_prompt.txt`：用于 Prompt 编辑和读取。



---

## 🖱️ 二、 核心功能：智能划词与剪贴板管理

脚本会在后台实时监听你的鼠标拖拽（划词）和剪贴板变化，智能弹出对应的操作悬浮窗。

### 1. 划词 / 复制文本

* **触发方式**：用鼠标左键拖拽选中文本，或直接按 `Ctrl+C` 复制文本。
* **防误触机制**：在 Chrome/Edge 的标签栏区域、VSCode 的侧边栏区域划动，或按住 `Ctrl/Shift/Alt` 时**不会触发**（已完美适配 2K 屏幕与 125% DPI 缩放）。
* **出现图标**：
* 📝 **Prompt 图标**：左键点击结合 `active_prompt.txt` 中的模板，直接将文本发送至 Gemini 新窗口；右键点击打开 Prompt 编辑器。
* ✨ **Gemini 图标**：左键点击将文本快速发送至当前的 Gemini 窗口（若无则新建）。
* 🗣️ **YouGlish 图标**：左键点击将文本发送至 YouGlish 进行发音查询。
* ✅ **Copied 图标**：提示复制成功的视觉反馈。



### 2. 截图 / 复制图片

* **触发方式**：使用任意截图工具（系统自带、Snipaste、PixPin 等）截图到剪贴板，或直接复制图片文件。
* **出现图标**：
* 会弹出 **Prompt 图标** 和 **Gemini 图标**，点击后可直接将图片+文本一起发送给 Gemini 识别。



> **💡 隐藏悬浮窗**：滑动鼠标滚轮（`WheelUp` / `WheelDown`）或按一下 `Esc` 键即可快速隐藏所有悬浮窗。

---

## ⌨️ 三、 多击触发引擎 (Multi-Tap)

通过连续快速敲击同一按键触发特定功能，既不占用日常修饰键，又能实现极速操作。

| 连续敲击按键 | 敲击次数 | 触发功能说明 |
| --- | --- | --- |
| **空格 (Space)** | 4次 | 删除前 4 个字符，并发送组合键 `Ctrl+Shift+7` |
| **N** | 5次 | 删除前 5 个字符，并极速启动系统 **记事本 (Notepad)** |
| **Z** | 3次 | 删除前 3 个字符，并作为独立应用打开 **知乎 (Zhihu)** |
| **R** | 3次 | 删除前 3 个字符，并作为独立应用打开 **Reddit** |
| **Y** | 3次 | 删除前 3 个字符，并作为独立应用打开 **YouTube** |
| **E** | 3次 | 删除前 3 个字符，并极速启动 **Everything** 搜索工具 |
| **A** | 3次 | 删除前 3 个字符，并极速启动 **Anytxt** 全文搜索 |
| **G** | 4次 | 删除前 4 个字符，强制**将当前剪贴板内容上传至 Gemini** |
| **D** | 4次 | 删除前 4 个字符，**强制清空剪贴板**（🗑️） |
| **鼠标右键** | 4次 | 发送 `Esc` 后延迟发送 `Enter`（↩️） |

---

## 🚀 四、 反引号 ( ` ) 组合快速启动

使用键盘左上角的 **反引号 (`)** 作为前导键，配合其他按键实现应用和网页的秒级启动。*(注：单独按 ` 会输出反引号，`Shift + `` 会输出波浪号 `~`)*

| 快捷键组合 | 目标程序 / 网页 | 快捷键组合 | 目标程序 / 系统功能 |
| --- | --- | --- | --- |
| **` + 1** | Chrome / Edge 浏览器 | **` + 8** | Telegram (自动探测路径) |
| **` + 2** | Gemini (PWA 模式) | **` + 9** | 运行：30秒后重启脚本 (`.bat`) |
| **` + 3** | Github (PWA 模式) | **` + 0** | 运行：30秒后关机脚本 (`.bat`) |
| **` + 4** | 哔哩哔哩 (PWA 模式) | **` + N** | 运行：切换有线网口状态 (`.bat`) |
| **` + 5** | YouTube (PWA 模式) | **` + S** | 运行：关闭安全中心设置 (`.bat`) |
| **` + 6** | YouGlish (PWA 模式) |  |  |
| **` + 7** | 抖音 (PWA 模式) |  |  |

> **提示**：以上网页均采用 Chrome/Edge 的 `--app` 模式启动，提供无边框的沉浸式体验。

---

## 🛡️ 五、 窗口管理与系统增强

这部分包含了一些极为强悍的系统级快捷操作和防呆设计。

### 1. 记事本自动保存 (Auto-Save)

* 当系统 **记事本 (Notepad)** 处于激活状态，且检测到标题栏有 `*` 或 `•`（表示有未保存的修改）时，只要键盘空闲超过 2 秒，脚本会自动帮你按下 `Ctrl+S` 进行保存，防止数据丢失。

### 2. 暴力查杀单个窗口 (Window Kill)

* **操作方式**：长按 `Esc` 键（超过 400 毫秒） + **鼠标左键点击** 任意目标窗口。
* **功能**：强制发送关闭信号，瞬间关闭当前鼠标悬停的单个窗口。
* **优势**：已优化底层逻辑，不会像强杀进程那样导致整个浏览器的其他网页一起崩溃。
* **注意**：长按 `Esc` 键的系统默认连发行为已被脚本底层拦截，不会再弹出恼人的 "71 hotkeys" 报错弹窗。

### 3. 一键唤醒幽灵窗口

* **快捷键**：`Win + J`
* **功能**：强制扫描系统中处于“最小化”状态的所有常规窗口，并将它们全部恢复显示（`WinRestore`）。已智能过滤系统底层不可见窗口，防止桌面 UI 错乱。

### 4. 独立功能组合键

* **`Win + Shift + G`**：当剪贴板中有图片或文本时，直接触发 Gemini 上传流程（跳过鼠标划词）。
* **`Ctrl + Shift + Y`**：将当前剪贴板文本强制送入 YouGlish 检索。

---

## 📝 六、 基础键位映射表 (F区按键)

对键盘顶部的 F 区按键进行了全局重映射，使其变为常用的高频编辑快捷键：

* **F1** = 撤销 (`Ctrl + Z`)
* **F3** = 粘贴 (`Ctrl + V`)
* **F4** = 全选 (`Ctrl + A`)
* **F5** = 复制 (`Ctrl + C`)
* **F6** = 退出/关闭标签 (`Alt + Q`)
* **F8** = 发送特定组合键 (`Ctrl + Shift + 8`)
* **F9** = 发送特定组合键 (`Ctrl + Shift + 9`)
* **F10** = 打印/特定调用 (`Ctrl + Shift + P`)
* **F11** = 关闭当前页面/窗口 (`Ctrl + W`)
* **F12** = 强制中止/退出 (`Ctrl + Shift + Esc`)

*(其他补充映射：`End` -> 选中到行尾；`PgUp` -> `Ctrl+Enter`；`PgDn` -> 粘贴 `Ctrl+V`)*

---

### 🔄 维护与热重载

当你修改了此 AHK 脚本的源代码并在编辑器中处于激活状态时，按下 **`Ctrl + S`**，脚本不仅会保存代码，还会**自动重新加载并重启自身**，并弹出 "🚀 脚本已成功加载新参数！" 的提示，极大提升调试效率。
